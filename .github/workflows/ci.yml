# SPDX-License-Identifier: AGPL-3.0-or-later
name: WAT CI

permissions: read-all

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1

      - name: Install WABT
        run: |
          wget -q https://github.com/WebAssembly/wabt/releases/download/1.0.34/wabt-1.0.34-ubuntu.tar.gz
          echo "42342d6a1227a9ff27b4e4f0068d90f5c6581c81ff66889c36a2b1dbe2c4323c  wabt-1.0.34-ubuntu.tar.gz" | sha256sum -c -
          tar xzf wabt-1.0.34-ubuntu.tar.gz
          echo "$PWD/wabt-1.0.34/bin" >> $GITHUB_PATH

      - name: Compile WAT to WASM
        run: |
          if [ -f src/wagasm-ssg.wat ]; then
            wat2wasm src/wagasm-ssg.wat -o src/wagasm-ssg.wasm
            echo "WAT compiled successfully!"
          fi

      - name: Validate WASM
        run: |
          if [ -f src/wagasm-ssg.wasm ]; then
            wasm-validate src/wagasm-ssg.wasm
            echo "WASM validation passed!"
          fi

  language-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1

      - name: Verify WAT-only in src/
        run: |
          echo "Checking for ABSOLUTELY FORBIDDEN languages in src/..."
          forbidden=$(find src/ -type f \( -name "*.ts" -o -name "*.js" -o -name "*.rs" -o -name "*.py" \) 2>/dev/null || true)
          if [ -n "$forbidden" ]; then
            echo "ERROR: Found FORBIDDEN language files in src/:"
            echo "$forbidden"
            echo ""
            echo "wagasm-ssg MUST be pure WAT. No TypeScript, no AssemblyScript, no Rust!"
            exit 1
          fi
          echo "OK: Only WAT files in src/"

      - name: Verify host runtime is I/O only
        run: |
          echo "Checking host runtime doesn't contain SSG logic..."
          if [ -f runtime/host.ts ]; then
            if grep -E 'generate|template|markdown|parse|route' runtime/host.ts; then
              echo "WARNING: Host runtime may contain SSG logic!"
              echo "The host should ONLY provide I/O imports."
            fi
          fi
